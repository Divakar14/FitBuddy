server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fitness_user_db
    username: root
    password: Admin@2026

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/